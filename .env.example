# Backend base URL (no trailing slash). Browser must reach this host.
# Local Spring Boot default:
VITE_API_URL=http://localhost:8080

# If the UI is served from another origin in Docker, add that origin to the
# backend env CORS_ALLOWED_ORIGINS (comma-separated), not here.
